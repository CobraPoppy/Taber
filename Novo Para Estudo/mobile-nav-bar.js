
let borom = window.document.getElementById('btn-mobile')
let LML = window.document.getElementById('navga')
    function Clicar(){
        if(LML.classList.contains("active")) {
             LML.classList.remove('active')
        }
    }
    function ALA() {
    if(LML.classList.contains("active" )) {
         LML.classList.remove('active' )
    }
    else {LML.classList.add('active' )}

    }
   
